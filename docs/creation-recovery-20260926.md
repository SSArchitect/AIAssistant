# 2026-09-26 创作重复手动继续排查与修复

## 生产证据

- 运行版本确认为1e493f4。9月26日00:44:24和00:44:59，同一分镜在人物参考视图准备阶段失败，错误码为`media_reference_view_failed`，尚未提交生图。每次内部视图检测都连续三次返回ValidationError。
- 之前的场景图已在两次质量返工后通过。图片五次返工选优不是这两次中断的原因。
- Bridge原先将人物参考准备与图片执行稿整理失败标成不可恢复；自动执行器只对其他媒体连接错误恢复，因而结束流程，让用户再点击。
- 视图检测旧日志仅保存ValidationError类型，不能据此确定是哪个字段或边界框失败。旧纠正消息也没有具体字段位置或上一份输出，容易重复同一种格式错误。
- 9月25日21:00和21:15的规划先花约636秒、587秒才进入外层恢复；随后达到约915秒总时限。22:19一轮还有网盘工具失败、视频引用职责校验失败及模型APIError。旧恢复没有保留前一次已完成的分段节点，又从资料与清单开始。

## 变更

- 人物参考视图准备与图片执行稿整理加入自动恢复，最多5次，复用同一请求和提交键，不占用图片质量返工次数。与已提交媒体的2次连接恢复分别计数；停止或节点修改后不继续提交。
- 检测反馈包含字段位置、错误类型和上一份回复；日志仅保存字段位置与错误类型，不记录原图或模型输出。完整JSON代码块可解析，仍执行原有坐标、视角、完整区域和重叠校验，不猜测缺失视图。
- 上下文缓存不匹配返回不可恢复的幂等冲突，鉴权失败保留明确错误。
- 分段规划保存已完成节点、清单及实际只读工具观察，自动重试从中断节点续接。按账号、项目、对话、选择、锁定节点、资产内容及原方案绑定；改动任何输入不得复用。仍在全部节点完成、完整图校验通过后一次提交。
- 断点只缓存在当前Agent进程内，30分钟过期；单条2MiB、总量8MiB。进程重启、过期、逐出时退回正常规划，不宣称完成。新人工请求重置旧断点。本轮915秒总上限保持不变。
- 总时限耗尽明确提示超时，不再使用“检查默认对话模型配置”的兜底提示。

## 验证边界

已用本地合成图片与模拟模型覆盖错误反馈、JSON封装、恢复成功、重试上限、停止、预算独立、资料/节点续接、输入隔离及缓存淘汰。字段反馈相关三项回归在修复前失败，修复后通过。

未重放用户私有原图识别：自动审批以外部模型数据传输和可能产生费用为由拒绝了该诊断操作。历史日志缺少字段细节，因此不能断言生产六次ValidationError的具体字段；此次修复补齐恢复与诊断机制，不把合成测试等同真实作品已恢复成功。

最终验证：Go handlers/bridge局部通过；Python相关59项通过；`./scripts/test.sh` 全部通过（Go vet/test/build，Python1647项，Web363项及配置/import/discovery）。Android原生测试因无Java/SDK跳过，本次无前端或原生变更，不需要OTA。
