# AI 开发规则

本仓库的开发与测试约定以 [docs/development-and-operations.md](docs/development-and-operations.md) 为准。

## Feature 改动必须有单元测试

- 每次新增或修改 feature，必须在同一次改动中新增或更新自动化单元测试，覆盖本次改动可观察到的行为；没有对应单元测试的 feature 改动不算完成。
- 测试至少覆盖主要成功路径，以及本次改动涉及的关键边界或失败路径。修复 bug 时，必须先补充或同步提交能够复现问题的回归测试。
- 不得只用编译、语法检查、手工验证或端到端测试代替单元测试。如果代码难以单测，应先拆分依赖或提取可测试边界。
- 完成前先运行受影响模块的局部测试，再运行 `./scripts/test.sh`；交付时说明新增或更新了哪些测试，以及测试结果。
- 纯文档、注释或不改变运行行为的格式调整不要求新增单元测试，但仍需执行与改动相称的校验。
